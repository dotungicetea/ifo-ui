import type { NextPage } from "next";
import HeaderComponent from "../components/header";

const Home: NextPage = () => {
  return (
    <div>
      <HeaderComponent />
    </div>
  );
};

export default Home;
